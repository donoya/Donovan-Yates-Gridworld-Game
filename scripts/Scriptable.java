package scripts;

import data.*;
/**
 * Any extensions to Script can be instantiated via a key press in the game configurable in config/controls
 * 
 * @author (Donovan Yates)
 * @version (0.1)
 */
public abstract class Scriptable
{
    public Scriptable(GameWorld gameWorld)
    {
    }
}
